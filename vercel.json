{
  "version": 2,
  "builds": [
    {
      "src": "pubspec.yaml",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "if [ -d \"/vercel/.flutter\" ]; then /vercel/.flutter/bin/flutter build web --release; else export PATH=\"$PATH:`pwd`/flutter/bin\" && git clone https://github.com/flutter/flutter.git && flutter channel stable && flutter upgrade && flutter config --enable-web && flutter pub get && flutter build web --release; fi",
        "outputDirectory": "build/web"
      }
    }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}